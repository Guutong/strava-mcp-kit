name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  publish:
    needs: ci
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      # OIDC token used by npm Trusted Publisher (no NPM_TOKEN needed
      # once the trusted publisher is configured on the npm side).
      id-token: write
    steps:
      - uses: actions/checkout@v4

      # Per npm Trusted Publisher docs: never cache deps in release builds, and
      # do not pass --provenance manually (npm attaches it automatically when
      # publishing via Trusted Publisher OIDC).
      - name: Setup Node.js (24 ships with npm >= 11.5)
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish to npm (Trusted Publisher OIDC)
        run: |
          echo "node $(node --version) / npm $(npm --version)"
          if [[ "${GITHUB_REF_NAME}" == *-* ]]; then
            echo "Pre-release tag detected, publishing under dist-tag 'next'."
            npm publish --access public --tag next
          else
            npm publish --access public
          fi

      - name: Extract release notes from CHANGELOG.md
        id: changelog
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v v="$VERSION" '
            BEGIN { capture = 0 }
            $0 ~ "^## \\[" v "\\]" { capture = 1; next }
            capture && /^## \[/ { exit }
            capture { print }
          ' CHANGELOG.md > release_notes.md

          if [[ ! -s release_notes.md ]]; then
            echo "No CHANGELOG section found for ${VERSION}; falling back to a stub."
            printf "Release %s.\n\nSee CHANGELOG.md.\n" "${VERSION}" > release_notes.md
          fi

          echo "--- release_notes.md ---"
          cat release_notes.md
          echo "------------------------"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body_path: release_notes.md
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
