# Static access token mode (simplest, expires in ~6 hours)
STRAVA_ACCESS_TOKEN=

# OR refresh-token mode (recommended; auto-renews access tokens)
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=

# Optional: override the API base URL
# STRAVA_API_BASE_URL=https://www.strava.com/api/v3
